# В этом файле конфигурируем запуск тестов на ГитХабе

# 1. Сейчас делаю дефолтный запуск тестов: тесты будут запускаться при каждом новом пуше изменений

#name: Automated tests    # задаём имя файлу
## ниже указываем, в какой ситуации (конструкции) это всё будет отрабатывать
#on:    # ON - это конструкция "если..."
#  push    # запускается, если произошёл пуш
## ниже описывается сам алгоритм запуска тестов:
#jobs:    # JOBS - это задача
#  tests:    # задача = запустить тесты
#    runs-on: ubuntu-latest    # указываем ОС, на которой будет запуск тестов
## ниже расписываем шаги, которые нужны для прогона теста
#    steps:    # в этом блоки даём название шагов и пояснение, что должно происходить на этом шаге
#      - name: Checkout repository    # шаг 1: скачиваем код внутрь нашего контейнера
#        uses: actions/checkout@v3    #        пользуемся готовыми/дефолтными шагами из функции "actions" гитхаба + через @ указываем версию функции
#      - name: setup Python    # шаг 2: установить питон
#        uses: actions/setup-python@v4    # обращаемся также к заготовленному экшну: установка пайтона + также через @ указываем последнюю актуальную версию
#        with:    # в этом блоке указываем точные настройки того, что нужно установить
#          python-version: "3.10"    # пишем конкретную версию питона, которую нужно установить
#      - name: Install Chrome    # шаг 3: устанавливаем Хром (самописное действие, не заготовленное)
#        run: sudo apt-get install google-chrome-stable    # эта команда должна будет устанвить последнюю стабильную версию Хрома
#      - name: Install dependencies    # шаг 4: установка всех зависимостей в проект
#        run: pip install -r requirements.txt    # через терминал устанавливаем все необходимые зависимости в проект
#      - name: Run tests    # финальный шаг: Запуск тестов
#        run: pytest

# 2. Делаем кастомный запуск: (!!! РАСКОМЕНТИТЬ, Т.К. ЭТО РАБОЧИЙ ВОРКФЛОУ !!!)
name: Automated tests    # задаём имя файлу
# ниже указываем, в какой ситуации (конструкции) это всё будет отрабатывать
on:    # ON - это конструкция "если..."
  workflow_dispatch: # юзаем, если нужны какие-то кастомные настройки для запуска ()
    inputs: # внутри него расписываем, что должно происходить
      deployment_target: # тут описываем команду/цель
        description: Choose target
        required: true # делаем ТРУ, чтобы не было возможности запуска без какой-то опции "что хотим запустить: ждём выбор пользователя, что именно он хочет запустить"
        default: # опция, которая будет воспроизводится по дефолту: можем указать только какие-нибудь смоук-тесты и будут гоняться они
        type: choice # опция: способ выбора - чойс (селектор, из которого мы выбираем опции)
        options: # в этом параметре перечисляем все опции, которые у нас есть
          - first_test
          - second_test
          - all_test
# ниже описывается сам алгоритм запуска тестов:
jobs:    # JOBS - это задача
  tests:    # задача = запустить тесты
    runs-on: ubuntu-latest    # указываем ОС, на которой будет запуск тестов
# ниже расписываем шаги, которые нужны для прогона теста
    steps:    # в этом блоки даём название шагов и пояснение, что должно происходить на этом шаге
      - name: Checkout repository    # шаг 1: скачиваем код внутрь нашего контейнера
        uses: actions/checkout@v3    #        пользуемся готовыми/дефолтными шагами из функции "actions" гитхаба + через @ указываем версию функции
      - name: setup Python    # шаг 2: установить питон
        uses: actions/setup-python@v4    # обращаемся также к заготовленному экшну: установка пайтона + также через @ указываем последнюю актуальную версию
        with:    # в этом блоке указываем точные настройки того, что нужно установить
          python-version: "3.10"    # пишем конкретную версию питона, которую нужно установить
      - name: Install Chrome    # шаг 3: устанавливаем Хром (самописное действие, не заготовленное)
        run: sudo apt-get install google-chrome-stable    # эта команда должна будет устанвить последнюю стабильную версию Хрома
      - name: Install dependencies    # шаг 4: установка всех зависимостей в проект
        run: pip install -r requirements.txt    # через терминал устанавливаем все необходимые зависимости в проект
      - name: first test    # финальный шаг: Запуск тестов: в зависимости от выбора пользователя
        if: "github.event.inputs.deployment_target == 'first_test'" # делаем заглушку/условие, что если выбираем для запуска фёрст_тест, то секонд_тест не запускаются
        run: pytest tests_ui/test_GET_paper_page.py::test_get_paper_page # указываем имя теста, который нужно запустить
      - name: second test    # финальный шаг: Запуск тестов: в зависимости от выбора пользователя
        if: "github.event.inputs.deployment_target == 'second_test'"
        run: pytest tests_ui/test_POST_correct_authorization.py::test_correct_login # указываем имя теста, который нужно запустить
      - name: all test # запустятся все тесты
        if: "github.event.inputs.deployment_target == 'all_test'"
        run: pytest

# 3. Делаем кастомный запуск: (!!! РАСКОМЕНТИТЬ, Т.К. ЭТО РАБОЧИЙ ВОРКФЛОУ !!!)
# 📌 Название воркфлоу, которое будет отображаться во вкладке Actions на GitHub
#name: 🔄 Scheduled UI Tests
#
## 🔔 Триггеры: события, при которых GitHub автоматически запускает этот скрипт
#on:
#  # 📤 Запускать при каждом пуше (git push) в ветку main
#  push:
#    branches: [main]
#  # 🔀 Запускать при создании или обновлении Pull Request в ветку main
#  pull_request:
#    branches: [main]
#  # ⏰ Запускать автоматически по расписанию (используется синтаксис cron)
#  schedule:
#    # 🕒 Формат: минута час день_месяца месяц день_недели (ВСЕГДА в часовом поясе UTC!)
#    # '0 6 * * 1-5' → каждый будний день (пн-пт) в 06:00 UTC, что равно 09:00 МСК
#    - cron: '0 6 * * 1-5'
#  # 🖱️ Добавить кнопку "Run workflow" в интерфейс GitHub для ручного запуска
#  workflow_dispatch:
#
## 📦 Блок с задачами (jobs), которые будут выполняться на виртуальной машине GitHub
#jobs:
#  # 🏷️ Имя первой и единственной задачи в этом воркфлоу
#  tests:
#    # 💻 Операционная система раннера (виртуальной машины). ubuntu-latest = свежая Ubuntu
#    runs-on: ubuntu-latest
#    # ⏱️ Лимит времени выполнения задачи. Если тесты идут >30 мин, GitHub принудительно остановит их
#    timeout-minutes: 30
#
#    # 📝 Список шагов (steps), которые выполняются строго последовательно
#    steps:
#      # 📥 Шаг 1: Скачивание (клонирование) вашего репозитория в рабочую папку виртуальной машины
#      - name: 📥 Checkout code
#        # 🛠️ Встроенное действие GitHub, которое выполняет git clone
#        uses: actions/checkout@v4
#
#      # 🐍 Шаг 2: Установка нужной версии Python на виртуальную машину
#      - name: 🐍 Set up Python
#        # 🛠️ Официальное действие для скачивания и настройки Python
#        uses: actions/setup-python@v5
#        # ⚙️ Блок с параметрами для действия выше
#        with:
#          # 🔢 Версия Python. Должна совпадать с той, на которой вы разрабатываете локально
#          python-version: '3.12'
#
#      # 📦 Шаг 3: Установка всех внешних библиотек (selenium, pytest и т.д.)
#      - name: 📦 Install dependencies
#        # 💻 Команды терминала (bash), которые выполнятся по порядку
#        run: |
#          # 🔄 Обновляем pip до последней версии, чтобы избежать конфликтов зависимостей
#          python -m pip install --upgrade pip
#          # 📥 Читаем файл requirements.txt и устанавливаем все перечисленные в нём пакеты
#          pip install -r requirements.txt
#
#      # 🧪 Шаг 4: Непосредственный запуск автотестов через фреймворк pytest
#      - name: 🧪 Run tests (headless)
#        # 💻 Команда запуска. Флаг -v включает подробный вывод каждого теста в лог
#        run: pytest -v
#        # 🌍 Блок переменных окружения. Они будут видны вашему Python-коду через os.getenv()
#        env:
#          # 🔐 Безопасная подстановка логина из раздела Secrets репозитория (не хранится в коде!)
#          TEST_EMAIL: ${{ secrets.TEST_EMAIL }}
#          # 🔐 Безопасная подстановка пароля из раздела Secrets репозитория
#          TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}
#
#      # 📸 Шаг 5: Сохранение скриншотов/логов, если тесты завершились с ошибкой
#      - name: 📸 Upload screenshots on failure
#        # ⚠️ Условное выполнение: шаг сработает ТОЛЬКО если предыдущий шаг (запуск тестов) упал
#        if: failure()
#        # 🛠️ Действие для загрузки файлов в артефакты текущего запуска
#        uses: actions/upload-artifact@v4
#        # ⚙️ Параметры для загрузки
#        with:
#          # 📁 Имя архива, который появится в интерфейсе Actions для скачивания
#          name: test-screenshots
#          # 📂 Путь к папке, куда ваш код сохраняет скриншоты (относительно корня репо)
#          path: screenshots/